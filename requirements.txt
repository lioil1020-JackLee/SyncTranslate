# SyncTranslate runtime dependencies
# This file mirrors pyproject.toml for pip-based installs.

# Core application runtime
comtypes>=1.4.16
edge-tts>=7.2.7
miniaudio>=1.61
numpy>=2.4.3
pycaw>=20251023
pyside6>=6.10.2
pyyaml>=6.0.3
soundcard>=0.4.5
sounddevice>=0.5.5

# Local ASR runtime
faster-whisper>=1.2.1

# Preferred install methods:
#   uv sync --extra local
#   python -m pip install -r requirements.txt
