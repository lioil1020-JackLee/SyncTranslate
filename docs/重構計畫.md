# SyncTranslate 重構計畫

> 文件建立日期：2026-04-29  
> 最後更新：2026-05-01（P0 + P1（全部）+ P2 + P3 全部完成；P4 評估後略過）  
> 分析範圍：`app/` 全部 Python 原始檔（共 78 個，不含 `__init__.py`）  
> 原則：**不影響任何現有功能，只做刪除死代碼、合併微型檔案、拆分巨型檔案。**

---

## 一、現況總覽

| 層級 | 目錄 | 主要問題 |
|------|------|---------|
| UI | `app/ui/pages/` | `local_ai_page.py`（1941 行）、`main_window.py`（1065 行） 過大 |
| Application | `app/application/` | `audio_router.py`（742 行）包含太多責任 |
| Infra / ASR | `app/infra/asr/` | `worker_v2.py`（966 行）；`audio_pipeline/` 下有 6 個微型檔案（10–45 行） |
| Infra / Audio | `app/infra/audio/` | `playback.py`（728 行）邏輯混雜；`playback_queue.py`（668 行） |
| Infra / Config | `app/infra/config/` | `schema.py`（606 行）`from_dict()` 佔半本；`settings_store.py`（513 行）migration 邏輯超長 |
| Infra / Logging | `app/infra/logging/` | `runtime_logger.py` 整個模組未被任何外部檔案 import（死模組） |
| Domain | `app/domain/` | `transcript_models.py`（17 行）、`events.py`（26 行）、`constants.py`（39 行）可整併 |
| Bootstrap | `app/bootstrap/` | `runtime_paths.py`（14 行）、`runtime_orchestrator.py`（29 行）過小 |

---

## 二、問題分類

### 2.1 確認的死代碼 / 死模組

| 位置 | 內容 | 確認方式 |
|------|------|---------|
| `app/infra/logging/runtime_logger.py` | 整個模組；`get_runtime_logger()`、`init_runtime_logger()` 均無外部 import | 全專案 grep（含 tests/、tools/）無任何 `from app.infra.logging` 或 `import runtime_logger` |
| `local_ai_page.py:1897` `_apply_group_heights()` | 定義了方法但全專案無任何呼叫點 | 全專案 grep `_apply_group_heights` 只找到定義本身 |

### 2.2 行數過多的巨型檔案（需拆分）

| 檔案 | 行數 | 拆分策略 |
|------|------|---------|
| `app/ui/pages/local_ai_page.py` | 1941 | 見 §3.1 |
| `app/ui/main_window.py` | 1065 | 見 §3.2 |
| `app/infra/asr/worker_v2.py` | 966 | 見 §3.3 |
| `app/infra/audio/playback.py` | 728 | 見 §3.4 |
| `app/infra/config/schema.py` | 606 | 見 §3.5 |
| `app/infra/config/settings_store.py` | 513 | 見 §3.6 |
| `app/infra/translation/lm_studio_adapter.py` | 513 | 見 §3.7 |

### 2.3 微型檔案（可合併）

| 檔案群組 | 檔案列表 | 合併目標 |
|---------|---------|---------|
| ASR audio pipeline stages | `identity.py`(10)、`highpass.py`(41)、`noise_reduction.py`(41)、`loudness.py`(45)、`music_suppression.py`(33) | `audio_pipeline/stages.py` |
| Domain 模型 | `transcript_models.py`(17)、`events.py`(26) | 合入 `domain/models.py` |
| Bootstrap 工具 | `runtime_paths.py`(14) | 合入 `bootstrap/app_factory.py` |
| ASR 工廠 | `factory.py`(19) | 合入 `infra/asr/manager_v2.py` 的 `create_asr_manager()` |
| TTS engine | `engine.py`(18) | 合入 `tts/playback_queue.py` 頂部 |
| Application orchestrator | `runtime_orchestrator.py`(29) | 合入 `application/session_service.py` |

### 2.4 重複代碼模式（需萃取）

| 位置 | 問題描述 |
|------|---------|
| `local_ai_page.py` `apply_config()` / `update_config()` | zh_asr / en_asr 雙通道設定代碼幾乎完全對稱，共重複約 160 行。應萃取 `_apply_asr_channel_config(channel, asr_config)` / `_update_asr_channel_config(channel)` 方法。 |
| `local_ai_page.py` 雙通道 LLM 設定 | 同上，`zh_en_llm`/`en_zh_llm` 對稱代碼重複約 80 行 |
| `schema.py` `from_dict()` | `asr_channels.local` 與 `.remote` 的解析邏輯完全相同，應抽成 `_parse_asr_config(raw, fallback)` 靜態方法 |
| `settings_store.py` `migrate_legacy_config()` | 逐欄位 deepcopy 賦值約 120 行可用 dict 更新循環簡化 |

---

## 三、逐項拆分方案

### 3.1 `local_ai_page.py`（1941 行 → 約 3 個檔案）

**問題：**
- 包含所有 UI 建立（`_build_*` 方法）、所有 config 雙向映射（`apply_config` / `update_config`）、所有 preset 邏輯（`_apply_experience_preset`）、UI 工具方法
- `_apply_group_heights()` 是死代碼（無任何呼叫點）

**拆分後：**
```
app/ui/pages/local_ai_page.py          ← 主體，保留 LocalAiPage class（~750 行）
app/ui/pages/_local_ai_preset.py       ← ExperiencePresetMixin（preset 邏輯 ~400 行）
app/ui/pages/_local_ai_builders.py     ← 所有 _build_* 方法（~500 行）
```

> 以 Mixin 方式分離，不改變外部 API：`LocalAiPage(ExperiencePresetMixin, QWidget)`

**同步刪除：**
- 第 1897–1938 行 `_apply_group_heights()` 方法（確認死代碼）

### 3.2 `main_window.py`（1065 行 → 約 2 個檔案）

**問題：**
- 視窗初始化、標籤頁組裝、session 控制、config 熱更新、錯誤節流、TTS 狀態回呼全混在一起

**拆分後：**
```
app/ui/main_window.py                  ← MainWindow class（視窗骨架 + 組裝，~450 行）
app/ui/_window_callbacks.py            ← 所有 on_* / _on_* 回呼方法（~400 行，Mixin）
```

### 3.3 `worker_v2.py`（966 行 → 約 2 個檔案）

**問題：**
- `SourceRuntimeV2` class 包含核心 ASR streaming 邏輯、幻覺過濾、統計計算、自適應調整等多個責任

**拆分後：**
```
app/infra/asr/worker_v2.py             ← SourceRuntimeV2 核心（start/stop/process，~550 行）
app/infra/asr/_hallucination_filter.py ← 幻覺過濾函數與常數（_SHORT_TAIL_*、_TAIL_*、_CJK_* + drop_reason，~120 行）
```

> `worker_v2.py` 從 `_hallucination_filter` import，外部介面不變。

### 3.4 `playback.py`（728 行 → 約 2 個檔案）

**問題：**
- `AudioPlayback` 同時包含 soundcard/sounddevice 裝置搜尋、串流播放、passthrough 串流邏輯

**拆分後：**
```
app/infra/audio/playback.py            ← AudioPlayback class（主邏輯，~450 行）
app/infra/audio/_device_selector.py   ← 裝置候選搜尋私有函數（~150 行）
```

### 3.5 `schema.py`（606 行 → 約 2 個檔案）

**問題：**
- `AppConfig.from_dict()` 佔了約 350 行，且 local/remote 通道解析完全對稱

**方案：**
```
app/infra/config/schema.py             ← 所有 @dataclass 定義（保留，~250 行）
app/infra/config/_schema_parser.py    ← AppConfig.from_dict() 的實作邏輯（~300 行）
```

> `AppConfig.from_dict()` 保留 classmethod 入口，內部呼叫 `_schema_parser` 的 `parse_app_config(raw)`。

**同步優化：**
- 萃取 `_parse_asr_channel(raw, fallback)` 靜態輔助函數，消除 local/remote 解析重複。

### 3.6 `settings_store.py`（513 行）

**問題：**
- `_normalize_external_config_keys()`（~100 行）、`_present_external_config_keys()`（~90 行）、`migrate_legacy_config()`（~200 行）三個大函數擠在一起

**方案：**
```
app/infra/config/settings_store.py         ← 公開 API：load_config / save_config（~100 行）
app/infra/config/_config_migration.py      ← migrate_legacy_config + is_legacy_config（~250 行）
app/infra/config/_config_serialization.py ← normalize / present external keys（~150 行）
```

### 3.7 `lm_studio_adapter.py`（513 行）

**問題：**
- 包含 HTTP 客戶端、prompt 建構、streaming 解析、批次翻譯邏輯

**方案：**
```
app/infra/translation/lm_studio_adapter.py ← LmStudioClient + translate()（~300 行）
app/infra/translation/_prompt_builder.py  ← prompt 建構函數（~130 行）
app/infra/translation/_stream_parser.py   ← streaming token 解析（~80 行）
```

---

## 四、合併小檔案方案

### 4.1 ASR audio pipeline stages 合併

**現況（5 個微型檔案 + identity.py）：**
```
audio_pipeline/identity.py          (10 行)
audio_pipeline/highpass.py          (41 行)
audio_pipeline/noise_reduction.py   (41 行)
audio_pipeline/loudness.py          (45 行)
audio_pipeline/music_suppression.py (33 行)
```

**合併為：**
```
audio_pipeline/stages.py            (約 170 行)
```
保留各 class 名稱不變。`frontend_chain.py` 的 import 路徑從 `from .highpass import ...` 改為 `from .stages import ...`。`audio_pipeline/__init__.py` 重新 export 所有 class，對外路徑不變。

### 4.2 Domain 模型合併

**現況：**
```
domain/transcript_models.py         (17 行)
domain/events.py                    (26 行)
```

**合併為：**
```
domain/models.py                    (約 43 行)
```

同步更新 `domain/__init__.py`，向後相容 import。

### 4.3 Bootstrap runtime_paths 合併

`runtime_paths.py`（14 行）只有 `runtime_logs_dir()` 一個函數，移入 `app_factory.py` 頂部，對外仍可透過 `from app.bootstrap.runtime_paths import runtime_logs_dir` 存取（在 `runtime_paths.py` 留 shim）。

### 4.4 ASR factory 合併

`factory.py`（19 行）的 `create_asr_backend()` 移入 `manager_v2.py`，`factory.py` 留 shim。

### 4.5 TTS engine 合併

`tts/engine.py`（18 行）的 `create_tts_engine()` 移入 `tts/playback_queue.py` 頂部，`engine.py` 留 shim。

### 4.6 Runtime orchestrator 合併

`runtime_orchestrator.py`（29 行）的 `RuntimeFacade` 移入 `session_service.py`，`runtime_orchestrator.py` 留 shim。

---

## 五、死代碼刪除清單

| 目標 | 說明 | 風險評估 |
|------|------|---------|
| `app/infra/logging/runtime_logger.py` | 全專案（含 tests/、tools/）無任何外部 import，可直接刪除整個目錄 | ✅ 低：全面 grep 確認無引用 |
| `app/infra/logging/__init__.py` | 隨 runtime_logger.py 一併刪除 | ✅ 低 |
| `local_ai_page.py` `_apply_group_heights()` 方法（第 1897–1938 行） | 全專案 grep 無呼叫點 | ✅ 低：grep 確認 |

---

## 六、執行優先順序

> 建議按以下順序進行，每階段完成後執行 `pytest` 確認測試通過。

| 階段 | 工作項目 | 預估影響行數 | 風險 | 狀態 |
|------|---------|------------|------|------|
| **P0 刪除死代碼** | 刪 `runtime_logger` 整個目錄；刪 `_apply_group_heights()` | -200 行 | 極低 | ✅ 已完成 |
| **P1 合併微型檔案** | §4.1 pipeline stages；§4.2 domain models；§4.4 factory→manager_v2；§4.5 engine→playback_queue；§4.6 orchestrator→session_service | -150 行 | 低（留 shim 確保 import 不斷） | ✅ 已完成 |
| **P2 消除重複代碼** | `local_ai_page` ASR 雙通道對稱萃取（`_apply_asr_channel_widgets` / `_update_asr_channel_from_widgets`）；`schema.py` `_parse_asr_config` | -200 行 | 中（需覆蓋測試） | ✅ 已完成 |
| **P3 拆分巨型檔案** | `worker_v2` 拆幻覺過濾；`schema.py` → `_schema_parser.py`；`settings_store.py` → `_config_migration.py` + `_config_serialization.py`；`lm_studio_adapter.py` → `_stream_parser.py` + `_prompt_builder.py` | -（行數不減，但分散） | 中（需更新 import） | ✅ 已完成 |
| **P4 拆分大型 UI 檔案** | `local_ai_page` Mixin 化；`main_window` 回呼分離 | -（行數不減，但分散） | 高（UI 類別 self 依賴太深，Mixin 切割風險大） | ⏩ 評估後略過 |

---

## 七、重構前後結構對照（P0+P1 完成後）

```
app/infra/logging/              ← 【刪除整個目錄】
app/infra/asr/audio_pipeline/
  stages.py                     ← 【新】合併 5 個微型 stage 檔案
  identity.py                   ← 【刪除】
  highpass.py                   ← 【刪除】
  noise_reduction.py            ← 【刪除】
  loudness.py                   ← 【刪除】
  music_suppression.py          ← 【刪除】
app/domain/
  models.py                     ← 【新】合併 transcript_models + events
  transcript_models.py          ← 【刪除後留 shim 或直接刪除】
  events.py                     ← 【刪除後留 shim 或直接刪除】
```

---

## 八、測試策略

重構過程中，每一步驟後執行：

```bash
# 單元測試（快速回歸）
pytest tests/ -x -q

# 若有修改 ASR worker
pytest tests/test_asr_streaming_and_profiles.py tests/test_asr_v2_endpointing.py -v

# 若有修改 config/schema
pytest tests/test_settings_contract.py tests/test_config_migration.py -v

# 若有修改 UI 頁面
pytest tests/test_ui_behavior.py -v
```

---

## 九、不在本計畫範圍內的事項

以下項目**暫不處理**，避免風險過高或超出本次範圍：

- `audio_router.py`（742 行）：邏輯高度耦合，需要較大重構，列入下一版計畫
- `live_caption_page.py`（825 行）：UI 行為複雜，需詳細 UI 測試後再拆分
- `playback_queue.py`（668 行）：thread-safety 敏感，需完整並發測試
- `faster_whisper_adapter.py`（449 行）：與外部 runtime 強耦合，不輕易動
- 任何涉及對外 API 簽名變更的重構

---

*本計畫由 GitHub Copilot 自動分析生成，執行前請人工確認每項刪除/合併的影響範圍。*
