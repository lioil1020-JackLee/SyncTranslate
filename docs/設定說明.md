# 設定說明

## 設定檔

主要設定檔：

- `config.yaml`
- `config.example.yaml`

設定 schema：

- [app/infra/config/schema.py](/e:/py/SyncTranslate/app/infra/config/schema.py)

設定讀寫與 migration：

- [app/infra/config/settings_store.py](/e:/py/SyncTranslate/app/infra/config/settings_store.py)

## 目前主要設定區塊

### `audio`

負責輸入 / 輸出裝置路由：

- `meeting_in`
- `microphone_in`
- `speaker_out`
- `meeting_out`

### `language`

描述本地與遠端的來源語言 / 翻譯目標語言。

| 欄位 | 預設 | 說明 |
|---|---|---|
| `meeting_source` | `en` | 遠端（會議）來源語言 |
| `meeting_target` | `zh-TW` | 遠端翻譯目標語言 |
| `local_source` | `zh-TW` | 本地來源語言 |
| `local_target` | `en` | 本地翻譯目標語言 |

> 注意：config migration 會自動將舊版欄位名稱（`remote_translation_target` / `local_translation_target`）對應至新欄位（`meeting_target` / `local_target`）。

### `asr_channels`

每通道 ASR 參數：

- `local`
- `remote`

常用欄位：

- `model`
- `device`
- `compute_type`
- `beam_size`
- `final_beam_size`
- `no_speech_threshold`
- `vad.*`
- `streaming.*`

注意：

- `model` 主要影響 `faster-whisper` 路徑
- 中文 faster-whisper 路徑不直接使用這裡的 model 名稱做 backend 選擇

### `llm_channels`

每通道翻譯參數。

### `tts`

共用 TTS 預設。

### `tts_channels`

每通道 TTS 覆寫。

### `runtime`

這是目前最重要的區塊。

## `runtime` 關鍵欄位

### ASR 主線

- `asr_pipeline`
  - 預設 `v2`
- `asr_v2_backend`
  - 目前主要是相容欄位
  - 實際 backend 仍由通道語言路由決定
- `asr_v2_endpointing`
  - endpointing backend 的設定入口

### 每通道語言入口

- `local_asr_language`
- `remote_asr_language`

這兩個欄位直接決定每通道實際使用的 ASR backend。

路由規則：

- `zh` / `zh-TW` / `zh-CN` / `cmn` / `yue` -> `faster_whisper_v2`
- 其他語言 -> `faster_whisper_v2`
- `auto` -> `faster_whisper_v2`
- `none` -> disabled

### 即時行為

- `sample_rate`
- `chunk_ms`
- `asr_pre_roll_ms`
- `stable_partial_min_repeats`
- `partial_stability_max_delta_chars`
- `asr_partial_min_audio_ms`
- `asr_partial_interval_floor_ms`
- `llm_partial_interval_floor_ms`
- `early_final_enabled`

### Queue

- `asr_queue_maxsize_local`
- `asr_queue_maxsize_remote`
- `llm_queue_maxsize_local`
- `llm_queue_maxsize_remote`
- `tts_queue_maxsize_local`
- `tts_queue_maxsize_remote`

### ASR 後處理

- `asr_final_correction_enabled`
- `asr_final_correction_context_items`
- `asr_final_correction_max_chars`

### Speaker diarization

- `speaker_diarization_enabled`
- `speaker_diarization_min_audio_ms`
- `speaker_diarization_max_speakers`
- `speaker_diarization_similarity_threshold`

目前這組功能仍建議視為實驗性。

### 其他

- `local_echo_guard_enabled`
- `local_echo_guard_resume_delay_ms`
- `remote_echo_guard_resume_delay_ms`
- `translation_exact_cache_size`
- `translation_prefix_min_delta_chars`
- `tts_cancel_policy`
- `tts_max_wait_ms`
- `tts_max_chars`
- `tts_drop_backlog_threshold`

## 目前建議理解方式

### 1. ASR backend 不再由 UI 的 engine 欄位主導

現在真正主導 backend 的是：

- `runtime.local_asr_language`
- `runtime.remote_asr_language`

### 2. `device` 是 requested device，不一定等於 effective device

即使 `config.yaml` 寫：

```yaml
device: cuda
```

實際執行時仍可能因為 Torch / CUDA 環境而回退為 `cpu`。  
要以 diagnostics / session report 中的：

- `device_effective`

為準。

### 3. 雙通道同時中文發話時，要特別注意 CPU bottleneck

如果：

- `local_asr_language = zh-TW`
- `remote_asr_language = zh-TW`
- `device_effective = cpu`

那麼即使邏輯上已避免共享模型互相污染，雙通道仍可能因吞吐量不足而延遲上升。

## 精簡外部 config

目前外部 config 以「保留有效設定」為主。  
不需要持久化的 runtime metadata、相容性衍生欄位、部分 legacy shared 欄位，會由程式在載入後補齊，而不是都寫回 `config.yaml`。

## 推薦流程

1. 在 `字幕頁` 決定每通道 ASR 語言
2. 在 `Local AI` 頁調整 device、VAD 與 queue
3. 用 diagnostics 確認：
   - `resolved_backend`
   - `device_effective`
   - `init_failure`
4. 再根據 session report 微調

---

## Phase 1-4 新增 runtime 欄位

### ASR 後處理

| 欄位 | 預設 | 說明 |
|---|---|---|
| `enable_postprocessor` | `true` | 啟用 TranscriptPostProcessor |
| `enable_partial_stabilization` | `true` | partial 穩定前綴（避免字幕回縮閃爍） |
| `glossary_enabled` | `false` | 啟用術語表套用 |
| `glossary_path` | `''` | YAML / JSON 術語表路徑 |
| `glossary_apply_on_partial` | `false` | partial 文字是否套用術語表 |
| `glossary_apply_on_final` | `true` | final 文字是否套用術語表 |

術語表格式範例（YAML）：

```yaml
- pattern: "Whisper"
  replace: "Whisper"
  case_sensitive: false
  mode: substring
- pattern: "LLM"
  replace: "LLM"
  mode: exact
```

### Structured Logging

| 欄位 | 預設 | 說明 |
|---|---|---|
| `enable_structured_logging` | `true` | 啟用背景 runtime logger |
| `runtime_log_format` | `jsonl` | 輸出格式（`jsonl` 或 `text`） |

結構化日誌輸出位置：`logs/runtime_*.jsonl`

### ASR Endpoint Profiles

| 欄位 | 預設 | 說明 |
|---|---|---|
| `asr_profile_local` | `default` | 本地通道 endpoint profile |
| `asr_profile_remote` | `default` | 遠端通道 endpoint profile |
| `streaming_profile_local` | `default` | 本地通道 streaming profile（保留欄位） |
| `streaming_profile_remote` | `default` | 遠端通道 streaming profile（保留欄位） |

可用 profile 名稱：

- `default`：一般室內環境（推薦起點）
- `meeting_room`：遠端會議 / 廣播語音
- `headset`：頭戴式麥克風近講
- `noisy_environment`：嘈雜環境
- `max_accuracy`：犧牲延遲換精度
- `low_latency`：即時字幕要求較嚴

### Streaming Degradation Policy

| 欄位 | 預設 | 說明 |
|---|---|---|
| `degradation_policy_enabled` | `true` | 啟用 streaming 降級保護 |

降級觸發條件：queue backlog 過高或 dropped chunks 累積過多。降級後會壓制 partial 發送頻率，降低 CPU / 翻譯壓力。



